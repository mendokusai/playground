<h1><%= @park.name %></h1>

<img src="https://maps.googleapis.com/maps/api/staticmap?center=<%=@park.name%>&zoom=<%= @zoom %> &size=600x300&maptype=roadmap
&markers=color:green%7Clabel:G%7C<%= @park.latitude %>,<%= @park.longitude %>"><br>



<h3><%= @park.address %>, <%= @park.city %>, <%= @park.state %></h3>
<h4>Description:</h4>
<p><%= @park.description %></p>

<h2>Comments</h2>

<%= render @park.comments %>


<h2>Add a comment:</h2>

<%= render 'comments/form' %>

<%= link_to "Edit", edit_park_path(@park) %>
<%= link_to 'Delete', park_path(@park),
method: :delete, data: {confirm: "Are your sure?" } %>
<%= link_to "Back", parks_path %>